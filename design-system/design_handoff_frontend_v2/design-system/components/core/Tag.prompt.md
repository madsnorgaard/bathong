Status chip in mono micro caps - used on photo frames, story cards and exhibition rows.

```jsx
<Tag>Photocall open</Tag>
<Tag variant="ink">In progress</Tag>
<Tag variant="outline">Always open</Tag>
```

Inside a `<Frame>` it auto-positions top-left. Keep labels to two or three words.
