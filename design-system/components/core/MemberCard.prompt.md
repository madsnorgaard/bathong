The membership object, used both as the web CTA panel and as the physical card visual.

```jsx
<MemberCard price="R 450" priceNote="Per year · student rate available" />
```

It is the one place signal fills a button by default. Keep the NPC footnote unless the partnership changes. Because it carries the mark, never use `Bathong!` here.
